export function getMaxProfit(profitAndLoss: number[], subArrayLength: number): number {
  const max = 0;

  /**
   * let window: number[] = [];
   * let sumOfWindow = 0;
   *
   * 배열 첨부터 끝까지 순회하면서 O(n)
   * 윈도우 사이즈보다 작으면 총합 구하지말고 계속 집어넣고
   * 윈도우 사이즈보다 크면 첫 요소 빼내와서 sum에서 제외하고 새 요소 집어넣고 sum에 더하기
   * 현재 sum값은 계속 max값에 집어넣고
   */
  return max;
}
