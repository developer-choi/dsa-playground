export function getMaxProfit(profitAndLoss: number[], subArrayMaxLength: number): number {
  let max = 0;

  /**
   * 현재 배열안에 가장 최대값을 알아낼 수 있으면 그냥 계속 슬라이딩 윈도우 돌릴텐데.
   * [a, b, c, d]
   * a, b, c, d, ab, bc, cd, abc, bcd, abcd 경우의 수 너무많음...
   */

  return max;
}
