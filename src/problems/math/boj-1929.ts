export function sieve(n: number): number[] {
  /**
   * 1부터 n까지 순회하면서
   * 소수인지를 판단하는 함수를 통해 소수를 판단 후
   * 반환.
   */
  return [];
}

function isSoSoo(value: number): boolean {
  /**
   * 1이면 false / 2이면 true / 2보다 큰 짝수면 false
   *
   * 그  외 나머지는 3부터 value값 까지 순회하면서
   * value % i === 0 이면 return false
   *
   * 순회 다 끝나면 return true
   */
  return false;
}
