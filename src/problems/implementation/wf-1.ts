/**
 * URL: src/problems/implementation/wf-1.md
 * Description: 아파트 관리비
 */

export function simulation(day: number, k: number): number[] {
  /**
   * isWeekend() 함수를 사전에 하나 만들고,
   *
   * 1월부터 12월이 고정이기 때문에, 12개월 값을 저장할 배열 하나 만들고,
   * map 순회하면서 각 월의 k일이 주말인지 isWeekend()를 통해 알아내서
   * true이면 1 / 아니면 0을 반환한다.
   */

  /**
   * isWeekend() 전달된 날짜가 주말인지 아닌지 판단하는 함수.
   * ### Parameter
   * - year를 모르기때문에, 기준이 될 1월 1일의 dayOfNewYear값
   * - 주말인지 아닌지를 알고싶은 날짜값. 형식은, targetDate (month, date)
   */

  return [];
}
