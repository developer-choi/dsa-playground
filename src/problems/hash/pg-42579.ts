/**
 * URL: https://school.programmers.co.kr/learn/courses/30/lessons/42579
 */

export function hash(genres: string[], plays: number[]): number[] {
    /**
   * const record: Record<string, {totalPlayCount: number; playedGenres: {count: number, index: number}[] } | undefined> = {};
   *
   * for(iet i = 0 ; i < genres.length ; i++) {
   *    동시에 2개 배열 순회
   *    하면서 record에 저장하기.
   *    - 기존에 있으면 배열에 추가하고 토탈 카운트 추가
   *    - 기존에 없으면 빈값 만들어서 현재값 넣기
   * }
   *
   * 그다음 정답배열 선언하고
   * 그다음 전체 정렬을 돌릴거임.
   * 그다음 객체 for in으로 순회하면서 전체 재생횟수로 한번 정렬을 돌리면서 그 안에서 또다시 count별로 max로 2번만 뽑기. 2순위까지 뽑는건 별도함수 추출예정.
   * 그 순회하면서 정답배열 하나씩 추가
   */
  return [];
}
