export function anagramUsingHashmap(text1: string, text2: string): boolean {
  /**
   * map 선언
   *
   * text1 돌면서 map에 글자 1개를 키값으로 count값을 넣고
   *
   * text2를 돌면서 map에 글자 1개를 키값으로 count만큼 -1씩 하면됨
   */

  // map 순회하면서 count가 전부 0인지를 반환하면됨.
  return true;
}
