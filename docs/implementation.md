## 데이터 크기 × 알고리즘 n값이 1억 초과 시 TLE

데이터 크기가 10만일 경우 O(n²)는 10억이므로 안 됨.
O(nlogn) / O(n) / O(logn) / O(1) 로 풀어야 한다.

## `||` / `&&` 연산자 단락평가

좌측값에 따라 우측이 실행되지 않을 수 있다.
조건문을 짤 때 우측 식에 부수효과(함수 호출 등)가 있다면 주의.
