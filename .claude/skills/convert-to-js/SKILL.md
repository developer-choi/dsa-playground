---
description: TypeScript 풀이를 Programmers 등 JS만 지원하는 사이트 제출용으로 변환한다.
---

TypeScript 풀이 파일을 JavaScript 제출용으로 변환하라.

## 규칙

1. 기존 파일 포매팅 바꾸지 말고 타입만 제거
2. 풀이 함수 이름은 `solution`으로 변경
3. 그 외 모듈 이름은 원래대로 유지
4. 주석 모두 삭제
5. 문제 URL을 읽고, 제한사항으로 인해 불필요한 검증 코드는 삭제
